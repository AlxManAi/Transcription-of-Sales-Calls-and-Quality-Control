1. Подключение Gmail для приема файлов с аудио звонков

    В модуле Gmail выберите действие "Watch Emails".
    Укажите фильтр (например, письма с вложениями аудиофайлов).
    Настройте доступ к вашему аккаунту Gmail через OAuth, предоставив разрешение Make.
    Убедитесь, что сценарий извлекает аудиофайлы из вложений.

2. Транскрибация аудио в текст

    Добавьте модуль Speech-to-Text API (например, Google Cloud Speech-to-Text).
    Подключите API-ключ Google Cloud.
    Настройте модуль для обработки аудиофайла из предыдущего шага.
    Выберите язык распознавания речи и убедитесь, что текстовая транскрипция возвращается в формате переменной.

3. Анализ текста звонка и отработка промпта

    Добавьте модуль AI (например, OpenAI или GPT).
    Создайте промпт, который включает:
        «Проанализируй текст звонка. Выяви основные проблемы и тревоги клиента».
    Передайте переменную с транскрипцией в поле промпта.
    Настройте возвращаемый результат в формате, удобном для дальнейшего использования (JSON, текст).

4. Анализ проблем и генерация путей решения

    Добавьте еще один модуль AI (например, OpenAI или GPT).
    Создайте промпт:
        «На основе текста звонка и выявленных проблем предложи пути решения для каждой из них».
    Передайте переменную с проблемами из шага 3 в промпт.
    Убедитесь, что результаты возвращаются в структурированном формате (список проблем и решений).

5. Подключение таблицы Google и внесение данных

    Добавьте модуль Google Sheets и выберите действие "Add a Row".
    Подключите Google-аккаунт через OAuth и выберите нужную таблицу.
    Укажите, в какие столбцы добавлять данные:
        Переменная с текстом звонка.
        Переменная с выявленными проблемами.
        Переменная с предложенными решениями.
    Для этого в каждом поле таблицы выберите соответствующую переменную из результатов предыдущих шагов.

Итог

Эта цепочка автоматизирует процесс обработки звонков: от получения аудио до анализа и сохранения решений в таблице. Убедитесь, что промпты точно отражают логику анализа. Если что-то не ясно, обращайтесь! 😊